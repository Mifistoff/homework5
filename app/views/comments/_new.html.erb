<%= form_for([@event, @event.comments.build]) do |f| %>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
    <%= f.hidden_field :commenter %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
